% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prmse.R
\name{prmse}
\alias{prmse}
\title{Tidy PRMSE and VAR Results}
\usage{
prmse(dstudy_obj, probs = c(0.025, 0.975))
}
\arguments{
\item{dstudy_obj}{A dstudy object from \code{\link{dstudy}()}}

\item{probs}{Numeric vector of length 2 specifying the quantile probabilities
for credible intervals. Default is \code{c(0.025, 0.975)} for 95\% CI.}
}
\value{
A tibble with one row per dimension and the following columns:
\describe{
\item{dim}{Dimension/subscale name}
\item{prmse_c_rel}{PRMSE(C→S_i) (relative) posterior mean}
\item{prmse_c_rel_LL, prmse_c_rel_UL}{PRMSE(C→S_i) (relative) credible interval limits}
\item{prmse_c_abs}{PRMSE(C→S_i) (absolute) posterior mean}
\item{prmse_c_abs_LL, prmse_c_abs_UL}{PRMSE(C→S_i) (absolute) credible interval limits}
\item{var_rel}{VAR (relative) posterior mean}
\item{var_rel_LL, var_rel_UL}{VAR (relative) credible interval limits}
\item{var_abs}{VAR (absolute) posterior mean}
\item{var_abs_LL, var_abs_UL}{VAR (absolute) credible interval limits}
}
}
\description{
Extract PRMSE(C→S_i) and VAR results as a data frame from a dstudy object.
This function provides a convenient way to access all VAR-related results
for multivariate models.
}
\details{
PRMSE(C→S_i) is the proportional reduction in mean squared error when using
the composite score to estimate the subscale's universe score.

VAR (Value Added Ratio) quantifies whether a subscale's own scores better
estimate its universe score than projecting from the composite score.
VAR > 1 suggests the subscale adds value and should be reported separately.
}
\examples{
\dontrun{
# Multivariate G-study
library(brms)
g_mv <- gstudy(
  bf(Score ~ 0 + Subtest + (0 + Subtest | r | Person)),
  data = data, backend = "brms"
)

# D-study
d_mv <- dstudy(g_mv, n = list(Person = 5))

# Get PRMSE and VAR results
prmse(d_mv)

# Get 90\% credible intervals
prmse(d_mv, probs = c(0.05, 0.95))
}
}
\seealso{
\code{\link{dstudy}()} for computing D-study results.
}
