% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_gtheory_data}
\alias{simulate_gtheory_data}
\title{Simulate Data for Generalizability Theory Analysis}
\usage{
simulate_gtheory_data(
  facets = NULL,
  formula = NULL,
  vc = NULL,
  sd_residual = 1,
  multivariate = FALSE,
  n_dims = 2,
  dim_names = NULL,
  format = c("wide", "long"),
  dim_var = "dimension",
  response_var = NULL,
  residual_cor = NULL,
  re_cor = NULL,
  nested = NULL,
  seed = NULL,
  prefix = ""
)
}
\arguments{
\item{facets}{A named list specifying the number of levels for each facet.
For example, \code{list(person = 50, item = 10)} creates a crossed design with
50 persons and 10 items. Names should be valid R variable names.}

\item{formula}{An optional lme4-style formula from which to derive the facet
structure. If provided, overrides \code{facets}. The formula should use
random effects syntax, e.g., \code{y ~ (1|person) + (1|item)}.}

\item{vc}{A named list of variance components (as variances, not SDs).
Names should match facet names or interactions (use \code{:} for interactions).
For example, \code{list(person = 1, item = 0.5, "person:item" = 0.3)}.}

\item{sd_residual}{Standard deviation of the residual error term. Default is 1.}

\item{multivariate}{Logical. If TRUE, generates multivariate data. Default is FALSE.}

\item{n_dims}{Number of dimensions (outcome variables) for multivariate data.
Default is 2. Ignored if \code{multivariate = FALSE}.}

\item{dim_names}{Character vector of dimension names. If NULL, dimensions
are named \code{y1}, \code{y2}, etc. for wide format.}

\item{format}{Character string specifying the output format: "wide" or "long".
Wide format has multiple outcome columns; long format has a dimension
indicator variable. Default is "wide".}

\item{dim_var}{Name of the dimension indicator variable for long format.
Default is "dimension".}

\item{response_var}{Name of the response variable. Default is "y" for
univariate and "score" for multivariate long format.}

\item{residual_cor}{A correlation matrix for residual errors across dimensions.
Must be a symmetric positive-definite matrix with dimensions matching \code{n_dims}.
NULL (default) implies independent residuals (identity matrix).}

\item{re_cor}{A named list of correlation matrices for random effects.
Names should match facet names. Each matrix should be symmetric positive-definite
with dimensions matching \code{n_dims}. NULL (default) implies independent random effects.}

\item{nested}{A named list specifying nesting relationships. Names are nested
facets, values are the facets they are nested within. For example,
\code{list(item = "person")} means items are nested within persons.}

\item{seed}{Optional random seed for reproducibility.}

\item{prefix}{Prefix for factor level names. Default is empty string,
producing levels like "1", "2", etc.}
}
\value{
A data frame with:
\itemize{
\item Columns for each facet (as factors)
\item One or more outcome columns (univariate: \code{y}, wide multivariate: \code{y1}, \code{y2}, ...,
long multivariate: \code{score} with \code{dimension} column)
\item For long format: a dimension indicator column
}
}
\description{
Generate simulated data for univariate and multivariate generalizability theory
(G-theory) analyses. Supports both wide-format and long-format multivariate
designs, with optional correlation structures for residual and random effects.
}
\details{
\subsection{Variance Component Specification}{

Variance components can be specified using either the \code{vc} parameter or
individual \verb{sd_*} parameters. The \code{vc} parameter takes a named list where
names correspond to facet names or interactions (use \code{:} to separate).
}

\subsection{Univariate Simulation}{

For univariate designs, the function generates data according to:
\deqn{y_{ijk...} = \mu + \epsilon_p + \epsilon_i + \epsilon_{pi} + \epsilon_{residual}}
where each \eqn{\epsilon} term is drawn from a normal distribution with
the specified variance component.
}

\subsection{Multivariate Simulation}{

For multivariate designs, correlations between dimensions can be specified:
\itemize{
\item \strong{Residual correlations}: Correlations between residual errors across dimensions
\item \strong{Random effect correlations}: Correlations between random effects for a specific facet
}

Correlations are implemented using Cholesky decomposition to ensure
proper multivariate normal distributions.
}

\subsection{Format Options}{
\itemize{
\item \strong{Wide format}: Multiple outcome columns (\code{y1}, \code{y2}, ...) - suitable for
\code{mvbind()} syntax in \code{gstudy()}
\item \strong{Long format}: Single outcome column with dimension indicator - suitable for
brms long-format models like \code{score ~ 0 + dimension + (0+dimension|facet)}
}
}
}
\examples{
# Univariate p x i design
data_uni <- simulate_gtheory_data(
  facets = list(person = 20, item = 10),
  vc = list(person = 1, item = 0.5, "person:item" = 0.3),
  sd_residual = 1
)

# Wide-format multivariate (2 dimensions)
data_wide <- simulate_gtheory_data(
  facets = list(person = 50, item = 10),
  vc = list(person = 1, item = 0.5),
  multivariate = TRUE,
  n_dims = 2,
  format = "wide"
)

# Long-format multivariate with correlations
cor_matrix <- matrix(c(1, 0.5, 0.5, 1), nrow = 2)
data_long <- simulate_gtheory_data(
  facets = list(person = 50, item = 10),
  vc = list(person = 1, item = 0.5),
  multivariate = TRUE,
  n_dims = 2,
  format = "long",
  residual_cor = cor_matrix
)

}
\seealso{
\code{\link[=gstudy]{gstudy()}} for conducting G-studies on the simulated data
}
