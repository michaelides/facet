% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample-sizes.R
\name{build_sample_size_info_per_dim_from_aov}
\alias{build_sample_size_info_per_dim_from_aov}
\title{Build Per-Dimension Sample Size Info from a mom Unbalanced Fit}
\usage{
build_sample_size_info_per_dim_from_aov(aov_model)
}
\arguments{
\item{aov_model}{An \code{aovfit} object produced by \code{fit_aov_multivariate_unbalanced()}.
Must have \verb{$n_per_dim}, \verb{$data}, \verb{$formula}, and \verb{$responses}.}
}
\value{
A named list of \code{sample_size_info} objects, one per dimension.
Returns \code{NULL} if \code{aov_model} is not an unbalanced aovfit.
}
\description{
For wide-format aov fits with \code{unbalanced = TRUE}, the per-response N is
stored as a named list on the aovfit object. This adapter reconstructs a
\code{sample_size_info} object for each dimension by re-running
\code{\link[=calculate_sample_size_info]{calculate_sample_size_info()}} on the non-NA subset of the original data
for that response. The result has the same shape as the long-format
\code{sample_size_info_per_dim}, so downstream code can consume it uniformly.
}
\keyword{internal}
