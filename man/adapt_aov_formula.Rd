% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimator-aov.R
\name{adapt_aov_formula}
\alias{adapt_aov_formula}
\title{Adapt aov Formula for the Method-of-Moments Estimator}
\usage{
adapt_aov_formula(formula, data, nested = NULL)
}
\arguments{
\item{formula}{A formula object.}

\item{data}{A data frame.}

\item{nested}{Optional named list overriding auto-detected nesting
(e.g., \code{list(Rater = "Task")}).}
}
\value{
A list with components:
\item{aov_formula}{The rewritten aov() formula string.}
\item{name_mapping}{Named list mapping the rewritten ANOVA source name
to the user-supplied component name.}
\item{nested_info}{The (possibly user-overridden) nesting relationships.}
\item{random_facet_specs}{The (possibly rewritten) random-effect specs.}
}
\description{
Inspects the formula and data to detect facets that are nested within other
facets in the data, and rewrites the aov Error() strata so the canonical
G-study specification works without spurious rank-deficiency warnings. This
is only used by the aov estimator; lme4 and brms are unaffected.
}
\details{
Behaviour:
\itemize{
\item Parses the formula with \code{\link[=parse_g_formula]{parse_g_formula()}} to obtain the user-supplied
random-effect specifications.
\item Calls \code{\link[=detect_nesting_patterns]{detect_nesting_patterns()}} (in R/sample-sizes.R) to find facets
that are nested in other facets in the data. The user may override
auto-detection by passing \code{nested} (the same argument accepted by
\code{\link[=gstudy]{gstudy()}}).
\item For each detected nested pair (A nested in B):
\itemize{
\item If the user wrote the explicit nested form \code{A:B} in the formula, the
helper leaves the spec alone and records the identity mapping
\code{A:B -> A:B} so downstream code can label the variance component.
\item If the user wrote the un-nested main effect \code{A} (the canonical
G-study shorthand for the A-within-B effect when labels are already
unique within B), the helper substitutes \code{A} with \code{A:B} in the
Error() term and records the mapping \code{A:B -> A} so the variance
component is labelled with the user-supplied name even though the
Error() decomposition uses the nested form.
}
\item Returns the rewritten aov formula string and a name mapping that
\code{\link[=compute_aov_vc_from_results]{compute_aov_vc_from_results()}} applies to the resulting ANOVA table.
}

Any \verb{Error() model is singular} warning from \code{\link[=aov]{aov()}} is suppressed by
the caller; for these designs the warning is spurious — the variance
components are still computed correctly, matching lme4 to several
decimals.
}
\keyword{internal}
