% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dstudy-helpers.R
\name{resolve_dim_n}
\alias{resolve_dim_n}
\title{Resolve Per-Dimension Sample Size for One Dimension}
\usage{
resolve_dim_n(dim_name, n_per_dim, n_global)
}
\arguments{
\item{dim_name}{Character: the dimension to extract.}

\item{n_per_dim}{The \code{n_per_dim} slot of \code{\link[=resolve_dstudy_sample_sizes]{resolve_dstudy_sample_sizes()}} output.}

\item{n_global}{The flat \code{n} list (per-facet, possibly scalar) used as fallback.}
}
\value{
A named list of facet -> n. Returns \code{n_global} unchanged when no
per-dim data is available for \code{dim_name}.
}
\description{
Extracts a per-dim named list of facet -> n from the \code{n_per_dim} structure
built by \code{\link[=resolve_dstudy_sample_sizes]{resolve_dstudy_sample_sizes()}}. Falls back to the global \code{n} for
facets not specified per-dim. Returns a list keyed by dim when iterating
in a loop. Mirrors the per-dim override pattern in
\code{\link[=calculate_coefficients_posterior]{calculate_coefficients_posterior()}} so non-posterior and posterior paths
agree on what "per-dim n" means.
}
\keyword{internal}
