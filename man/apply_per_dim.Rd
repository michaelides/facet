% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dstudy-helpers.R
\name{apply_per_dim}
\alias{apply_per_dim}
\title{Apply a Variance Function Per Dimension}
\usage{
apply_per_dim(vc, n, n_per_dim, n_tibble, fn, ...)
}
\arguments{
\item{vc}{Variance components tibble with a \code{dim} column.}

\item{n}{Named list of facet -> n (global n; per-dim n overrides apply).}

\item{n_per_dim}{The \code{n_per_dim} slot from \code{\link[=resolve_dstudy_sample_sizes]{resolve_dstudy_sample_sizes()}}.}

\item{n_tibble}{The \code{n_tibble} slot from \code{\link[=resolve_dstudy_sample_sizes]{resolve_dstudy_sample_sizes()}}.}

\item{fn}{A variance function such as \code{\link[=calculate_divided_variance]{calculate_divided_variance()}} or
\code{\link[=calculate_dstudy_variance]{calculate_dstudy_variance()}}.}

\item{...}{Additional arguments passed to \code{fn}.}
}
\value{
Tibble of variance components (per-dim, row-bound).
}
\description{
When \code{\link[=needs_per_dim_vc_loop]{needs_per_dim_vc_loop()}} is TRUE, splits the variance components
tibble by \code{dim} and calls \code{fn(vc_dim, n_dim, ...)} for each dimension,
then row-binds the results. Otherwise calls \code{fn(vc, n, ...)} once with
the full vc and global n. Used to give the non-posterior D-study paths
per-dim awareness without changing their signatures.
}
\keyword{internal}
