% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-ranef.R
\name{plot_ranef}
\alias{plot_ranef}
\title{Caterpillar Plot for Random Effects}
\usage{
plot_ranef(
  x,
  which = NULL,
  ci_level = 0.95,
  sort = TRUE,
  colors = NULL,
  ncol = NULL,
  ...
)
}
\arguments{
\item{x}{A gstudy object.}

\item{which}{Which random effect(s) to plot. If NULL (default), plots all
random effects in a multi-panel plot. Can be a character vector of
specific facet names, or a single facet name.}

\item{ci_level}{Confidence/credible interval level. Default is 0.95.}

\item{sort}{Logical; if TRUE (default), sort effects by estimate size.}

\item{colors}{Optional vector of two colors for points and error bars.}

\item{ncol}{Number of columns for multi-panel plot when which is NULL.
Default is NULL (automatic layout).}

\item{...}{Additional arguments (passed to ggplot2 theme functions).}
}
\value{
A ggplot object (invisibly).
}
\description{
Creates a caterpillar (dot plot with error bars) to display random effects
from a gstudy object. Works with both lme4 and brms backends, providing a
unified interface regardless of backend used.
}
\examples{
# Fit a G-study with lme4 (default) using the brennan dataset
g <- gstudy(Score ~ (1 | Person) + (1 | Task) + (1 | Rater),
  data = brennan
)

# Plot all random effects
plot_ranef(g)

# Plot only one specific random effect
plot_ranef(g, which = "Person")

\donttest{
# Fit G-study with brms for Bayesian credible intervals
g_brms <- gstudy(Score ~ (1 | Person) + (1 | Task),
  data = brennan,
  backend = "brms"
)

# Plot with brms (uses posterior credible intervals)
plot_ranef(g_brms)
}
}
