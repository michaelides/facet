% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coefficient-helpers.R
\name{compute_residual_divisor}
\alias{compute_residual_divisor}
\title{Compute Residual Divisor Excluding the Object of Measurement}
\usage{
compute_residual_divisor(residual_is, n, object_spec)
}
\arguments{
\item{residual_is}{Character string specifying residual composition
(e.g., "Person:Rater"). If NULL or empty, all facets in \code{n} are used
(excluding object).}

\item{n}{Named list of sample sizes for each facet.}

\item{object_spec}{Character vector of object component names.}
}
\value{
Numeric divisor (product of \code{n} for non-object facets in the residual).
}
\description{
Calculates the divisor for the residual variance component when scaling
for a D-study, excluding the object of measurement from the divisor.
This implements the generalizability-theory rule that the object of
measurement is NOT averaged over when rescaling the residual.
}
\details{
The residual is decomposed into its constituent facets (e.g.,
\code{"Person:Rater"} → \code{c("Person", "Rater")}). The divisor is the product
of \code{n[[f]]} for facets that are NOT the object of measurement. For
example, with \code{residual_is = "Person:Rater"}, \code{object = "Person"}, and
\code{n = list(Rater = 4)}, the divisor is \code{4} (Rater only; Person excluded).
}
\keyword{internal}
