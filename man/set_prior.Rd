% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{set_prior}
\alias{set_prior}
\title{Define Priors for brms Models}
\usage{
set_prior(
  prior,
  class = "b",
  coef = "",
  group = "",
  resp = "",
  dpar = "",
  nlpar = "",
  lb = NA,
  ub = NA,
  check = TRUE,
  ...
)
}
\arguments{
\item{prior}{A character string defining a distribution in Stan language.}

\item{class}{The parameter class. Defaults to \code{"b"} (i.e., population-level effects).
See Details in \code{\link[brms:set_prior]{brms::set_prior()}} for other valid parameter classes.}

\item{coef}{For class \code{"b"}, the coefficient name.}

\item{group}{For class \code{"sd"} or \code{"cor"}, the grouping factor.}

\item{resp}{For multivariate models, the response variable.}

\item{dpar}{For distributional parameters, the parameter name.}

\item{nlpar}{For non-linear parameters, the parameter name.}

\item{lb}{Lower bound for parameters with lower bounds.}

\item{ub}{Upper bound for parameters with upper bounds.}

\item{check}{Logical; check if prior is valid for the model.}

\item{...}{Additional arguments passed to \code{\link[brms:set_prior]{brms::set_prior()}}.}
}
\value{
A brmsprior object; a data frame containing the prior specifications.
}
\description{
This function is a wrapper around \code{\link[brms:set_prior]{brms::set_prior()}} to allow
specifying priors for Bayesian models fit via the brms backend in gstudy.
See \code{\link[brms:set_prior]{brms::set_prior()}} for full documentation on parameter specifications.
}
\examples{
\dontrun{
# Define a prior for standard deviations of random effects
prior <- set_prior("normal(0, 1)", class = "sd", group = "person")

# Use with gstudy
g <- gstudy(score ~ (1 | person) + (1 | item),
  data = mydata,
  prior = prior,
  backend = "brms"
)
}
}
\references{
Paul Bürkner (2017). brms: An R Package for Bayesian Multilevel Models.
\emph{Journal of Statistical Software}, 80(1), 1-28.
\doi{10.18637/jss.v080.i01}
}
