# Find Residual Item

Returns the Residuals entry from the innermost (last) stratum. This is
the within-cell error, which is the correct residual variance for
G-study estimates.

## Usage

``` r
find_residual_item(anova_results)
```
