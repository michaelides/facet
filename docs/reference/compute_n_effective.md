# Compute Effective Sample Size for a Variance Component

Calculates the effective n for a given effect, accounting for nested vs
crossed designs.

## Usage

``` r
compute_n_effective(source, n_total, facet_n, random_facets, crossing_info)
```
