# Wrapper Functions for brms Formula Components

These functions are wrappers around brms formula components to allow
specifying complex models (like multivariate ones) via the brms
estimator in gstudy.
