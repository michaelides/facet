# Validate a gstudy Object

Checks that a gstudy object has the required components and they are
valid.

## Usage

``` r
validate_gstudy(x)
```

## Arguments

- x:

  An object to validate.

## Value

TRUE if valid, otherwise raises an error.
