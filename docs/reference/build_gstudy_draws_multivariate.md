# Build Multivariate gstudy Draws as Named List

Returns a named list of tibbles, one per dimension. Each tibble has the
same structure as univariate.

## Usage

``` r
build_gstudy_draws_multivariate(object, vc_draws, n, n_draws, components, dims)
```
