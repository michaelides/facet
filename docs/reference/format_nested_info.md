# Format Nested Info for Display

Creates a formatted string representation of nested effect information.

## Usage

``` r
format_nested_info(nested_info, indent = 1)
```

## Arguments

- nested_info:

  List from calculate_nested_sample_sizes().

- indent:

  Number of spaces for indentation (default 1).

## Value

Character vector of formatted lines.
