# Method of Moments Backend for G-Studies

Functions to fit G-study models using the method of moments (ANOVA-based
variance component estimation). This is an internal backend used by
[`gstudy()`](https://github.com/yourorg/facet/reference/gstudy.md).
