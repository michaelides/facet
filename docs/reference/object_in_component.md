# Check if Object is in Component

Determines whether the object of measurement appears in a variance
component (as main effect or part of an interaction).

## Usage

``` r
object_in_component(component, object)
```

## Arguments

- component:

  Character string naming the variance component.

- object:

  Character string naming the object of measurement.

## Value

TRUE if the object appears in the component.
