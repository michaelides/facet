# Generate Design Structure

Generate Design Structure

## Usage

``` r
generate_design_structure(facets, nested = NULL, prefix = "")
```

## Arguments

- facets:

  Named list of facet levels

- nested:

  Named list of nesting relationships

- prefix:

  Prefix for level names

## Value

Data frame with facet columns
